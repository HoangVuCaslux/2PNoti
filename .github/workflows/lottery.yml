name: Lottery Daily

on:

  schedule:

    - cron: '0 8 * * *'
    - cron: '35 9 * * *'
    - cron: '35 10 * * *'

  workflow_dispatch:

jobs:

  run:

    runs-on: ubuntu-latest

    steps:

      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5

        with:
          python-version: "3.11"

      - name: Install packages
        run: pip install -r requirements.txt

      - name: Run lottery report
        env:
          BOT_TOKEN: ${{ secrets.8595902209:AAE8uc01daiaWB82zRm1GtOrWFULjFKDJJU }}
          CHAT_ID: ${{ secrets.8702662620 }}
        run: python main.py
